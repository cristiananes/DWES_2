<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Astronautas</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">Lista de Astronautas</h1>
        <!-- Tabla de astronautas -->
        <table class="table table-hover table-sm">
            <thead class="table-dark">
                <tr>
					<th>id</th>
                    <th>Nombre</th>
                    <th>Edad</th>
                    <th>Nacionalidad</th>
                    <th></th>
                </tr>
            </thead>
            <tbody id="astronautTableBody">
                <!-- Filas generadas dinámicamente -->
               <tr th:each="a: ${astronautas}">
					<td th:text="${a.id}">id</td>
                    <td><a class="btn btn-primary" th:text="${a.nombre}" th:href="@{/astronauta/{id}(id=${a.id})}">Nombre</a></td>
                    <td th:text="${a.edad}">Edad</td>
                    <td th:text="${a.nacionalidad}">Nacionalidad</td>
                    <td><a href="#" type="button" class="btn btn-danger btn-sm" th:href="@{/astronauta/{id}(id=${a.id})}" >X</a></td>
                </tr>
            </tbody>
        </table>
    </div>

</body>
</html>
